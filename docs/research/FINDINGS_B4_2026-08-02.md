# Build 4 findings — FCS curve INTEGRATED to production via the proforma bridge (2026-08-02)

Registration + two amendments: REGISTRATION_B4_FCS_INTEGRATION_2026-08-02.md.
Scripts: b4_shadow.py (aborted mechanism, preserved), b4_amend2.py (shipped).

## Arc

Amendment 1 ABORTED at the registered sanity gate (scale incommensurability:
player-scale numbers vs percentile-scale unit grades; 87% phantom deltas —
zero rows written; the gate's existence saved ~150 units from ~+30-pt
nonsense). Amendment 2 rebuilt the mechanism on the REAL v2 bridge:
S17 curve grades injected into a faithful proforma replication (v2_ent =
curve + destination-conference offset; no re-shrink, no jump — the curve's
slope/intercept already do both jobs), unit aggregates re-percentiled
across the same 138-team field, formula-vs-formula Δpct transmitted through
the STANDARD sweep blend weights (DB 1/3, LB 0.40, others 0.50).

## Shadow (Amendment 2) — gates PASSED

148 affected units; post-blend deltas mean +0.29, SD 2.55; direction
57 up / 54 flat / 37 down (39% max vs 90% gate); 1.4% beyond ±8 (vs 10%
gate). Distribution matches S17's compressive curve: elite-FCS entrants
lift weak units (App State WRTE +17.9 pre-cap — Lofton; Cincinnati WRTE
+7.8 — Fenner), mediocre entrants drag units the prose had implicitly
over-credited (UCLA QB −9.2 pre-cap; Wake Forest RB −5.2). 17
identity-guard skips (spine name collisions) queued for manual read.

## Production integration

38 adjudication rows written (threshold |blend×Δpct| ≥ 2, cap ±8, conf
unchanged) → regen (38 unit grades changed, verified exact) → final_pass →
full chain rebuild. Board: mean |Δfinal| 0.097; movers: South Florida −0.7,
Sacramento State −0.6, UCLA −0.4 | Wyoming +0.38, Tulsa/ECU +0.29, Ohio
+0.27. Held book: 22 edges moved ≤1.1pp, ALL survive bars (Tulsa +17.1→
+18.0, ECU +11.6→+12.7, Wake +11.5→+10.5, Rutgers 15.6). NONE below 4%.

## Standing state

FCS entrants are now VISIBLE to the mechanical arm systematically — vibes
retired for the 201 curve-covered players; brackets remain only for
D2/D3 (6 pairs — unfittable, owner-predicted), JuCo/NAIA (no tape), and
unmatched names. December scoring (registered): 218 projections vs
realized; integrated-vs-pre units vs realized; bracket-only control group.
2027: native proforma integration (this bridge becomes the formula itself).
Manual queue: 17 identity-guard collisions.
