# Pre-registration — Study 13: rp board impact (2026-07-31)

Owner-commissioned. **Peek disclosure:** rp's existence/size is SEEN many times
(S6-F2 registered: +3.28/100pp, t 2.34, ΔR² 0.0096 FAIL materiality; S9 replication
+3.20 t 2.28; mid-band concentration +5.88 t 2.49; S12 horse races). S13 tests the
UNSEEN: does rp improve the historical BETTING BOARD — the test every factor has so
far failed. λ is FROZEN at the S6-F2 registered coefficient (+3.28 per 1.00 of
%PPA), centered within-year; no fitting on boards.

## Legs and bars

- **L1 (blanket):** rating' = sp + 3.28·(rp − year-mean). SBD DK openers 2022–24
  (probit engine, S7 conventions). PASS iff (i) MAE vs actual wins improves AND
  (ii) |d|≥1.0 zone side-rate within 5pp of consensus-alone.
- **L2 (conditional, theory-true per S9):** term applied ONLY to within-year
  middle-tercile (preseason SP+) teams. Same two bars. NOTE: this leg also audits
  the committed completion-screen mid-band rule, which was adopted from S9 without
  a board test.
- **L3 (position-split, the S6 registered retry — secondary):** replace overall
  %PPA with available passing/receiving/rushing return components (fields
  permitting); report component coefficients on the miss panel; claim only at
  t ≥ 2 per component with LOYO sign-stability. No board leg unless L1/L2 pass.
- **L4 (2026 frozen fold):** CFBD 2026 returning production is UNPOSTED (live pull
  0 rows). Freeze instead the tape-retention proxy (2026 portal-mode ret-share,
  s9 construction) to data/research/s13_rp2026_proxy.csv now; if CFBD posts %PPA
  before Sept 1, freeze that too at first pull (timestamped, both scored in
  December: 2026 miss ~ sp + rp-term).

## Decision rules (registered)

- L1 PASS and/or L2 PASS with zone intact → licenses REGISTERED adjustments to the
  BETTING layer now (screen weights/exclusions), and elevates the 2027 ratings
  candidate to presumptive. The 2026 RATINGS stay frozen regardless.
- L2 FAIL on zone dilution → the existing mid-band screen exclusion is flagged for
  review (its premise would be untested-and-failed at board level).
- All FAIL → the 2027 rp ratings candidate is demoted alongside; rp remains a
  panel-level curiosity like the rest of the disruption cluster.

## Limitations

Same reused panel/boards as S6–S12 (P4-heavy SBD, 3 seasons of boards); frozen λ
from an in-sample fit (disclosed; avoids board-fitting); 2026 fold uses a proxy
correlated-but-not-identical to %PPA (disclosed).
