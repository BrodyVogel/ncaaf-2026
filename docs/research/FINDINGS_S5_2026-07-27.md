# Study 5 findings — opener margin calibration (2026-07-27)

Prereg: `PREREGISTRATION_S5_2026-07-26.md` (committed before fit). Panel: 228 FBS-FBS
games, first-two-week windows 2021–25, preseason SP+ proxies, all with closing spreads.
Code: panel builder + `pipeline/research/price_openers.py`.

## Results vs bars

- **S5-A PASS**: b = 0.954 pooled, folds 0.908–0.984 — in (0,1), stable.
- **S5-B FAIL, in the happy direction**: LOYO margin MAE with fitted (b,h) is 12.799 vs
  12.711 for the engine's existing assumption (b=1, h=2.3) — fitting the calibration
  makes things *worse* out-of-sample because there is nothing to calibrate: preseason
  ratings transfer to opener margins essentially at face value (b ≈ 0.95 ± ~0.08,
  indistinguishable from 1). The premise that raw ratings need a game-level margin
  shrink is refuted for the opener window.
- **S5-C**: fitted h = 2.59 but fold-unstable → fall back to h = 2.3 per rule.
- **Report-only**: market shrink b_mkt = 0.895, h_mkt = 3.16 — the market compresses
  opener spreads ~10% relative to what results warranted on this panel; historically
  the RESULTS sided with fair. Residual SD 16.09 (vs the engine's generic 13.5) — used
  as the cover-prob sigma with half-point/push handling; empirical kernel ≈ normal at
  n=228, key-number structure unresolvable at this n. Tercile slopes: mid 0.86 / large
  0.95 (small-gap tercile meaningless at these n).

## Product consequence (owner sign-off items)

Per the registered gate, no FITTED layer ships — the validated translation is the
identity: **fair spread = final-rating diff + 2.3·site, cover probs at σ = 16.09.**
Pricing under two lenses per the registered rule: honest (b=1) and market-discount
(b_mkt = 0.895, h_mkt = 3.16); conviction = min ≥ +4% both (proposed). Support guard:
|fair| > 35 flagged EXTRAP (outside fitted panel; garbage-time dynamics unmodeled) —
report-only, not bettable v1. Board priced 2026-07-27 (53 posted games, weeks 0/1 +
GOTY): 12 inside-support ✓✓; snapshot in `outputs/opener_board_2026-07-27.txt`.
Awaiting owner: (1) ship-on-identity decision, (2) the 4% bar, (3) game stakes count
in full vs team season caps, (4) real two-way juice per bet (board priced at assumed
−110), (5) per-bet August-news freshness check is mandatory.

Registered limitation stands: b fitted on SP+ proxies; September paper-CLV log is the
live check on whether OUR ratings' game-level content matches the proxy's.
